[project]
name = "assetlock"
description = "Time-locked asset vault with release schedules"
authors = ["solidworkssa"]
telemetry = false
cache_dir = ".cache"

[contracts.assetlock]
path = "contracts/assetlock.clar"

[repl.analysis]
passes = ["check_checker"]
